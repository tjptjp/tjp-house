using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Xml;
using System;
using System.IO;

public class TimeManager//没用的
{
    /*  
  int TiXishu = 0;
  int Trabel = System.DateTime.Now.Minute + TimeIsMoney[Timerrrrr];  
    if (Trabel >= 60)
  {
      for (int i = Trabel; i > 0; i = i - 60)
      {
          if (i >= 60)
          {
              TiXishu++;
          }
          else
          {
              i = 0;
          }
      }
  }
  //DateTime d1 = new DateTime(System.DateTime.Now.Year, System.DateTime.Now.Month, System.DateTime.Now.Day, System.DateTime.Now.Hour + TiXishu,  - 60*TiXishu, System.DateTime.Now.Second);
  */

    /*   public void TimeWalker(int QuestNumBer)
    {
        Debug.Log("第" + QuestNumBer + "任务时间是：" + TimeIsMoney[QuestNumBer]);

    }
*/

}
